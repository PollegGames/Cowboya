/// <summary>
/// Stable identity of a cube stored or restored by the laboratory.
/// Serialized numeric values must not be reordered.
/// </summary>
public enum LaboratoryCubeType {
    White = 0,
    MaxHealth = 1,
    MaxEnergy = 2,
    EnergyRecharge = 3,
    AttackDamage = 4
}
